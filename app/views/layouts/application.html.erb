<!DOCTYPE html>
<html>
<head>
  <title>Lederfeier</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= stylesheet_link_tag params[:controller] %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
<!--   #1 line for bootstrap mobile first: -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <link href='http://fonts.googleapis.com/css?family=Codystar' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Sonsie+One' rel='stylesheet' type='text/css'>
</head>
<body>
  <div class="container">
    <div class="notice"><%= notice %><%= alert %></div>

<!-- #navbar with bootstrap -->
<nav class="navbar navbar-default navbar navbar-fixed-top" role="navigation" >
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <a class="navbar-brand" href="/events">Lederfeier</a>
  </div>
  <ul class="nav navbar-nav navbar-right">
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <i id="navbar-wrench" class="fa fa-cogs"></i></a>
        <ul class="dropdown-menu">
        <% if current_user.present? %>
          <li><%= link_to 'Your Account', edit_user_registration_path %></li>
          <li class="divider"></li>
          <li><%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %></li>
        <% else %>
          <li><%= link_to 'Register Now!', new_user_registration_path %></li>
          <li><%= link_to 'Sign in with Facebook', user_omniauth_authorize_path(:facebook) %></li>
          <li><%= link_to 'Sign In', new_user_session_path %></li>
        <% end %>
        </ul>
    </li>
  </ul>
</nav>
<!--  #bootstrap nav bar end -->

  <%= yield %>

  </div>
</body>
</html>
